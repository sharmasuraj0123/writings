{
  "section_id": "the-release-scripts",
  "section_file": "chromium/ci-cd-and-releases/_sections/11-the-release-scripts.html",
  "checked_at": "2026-09-23",
  "checker": "fact-check pass (dossier /home/coder/xo-projects/writings/chromium/_dossiers/cicd/ + /home/coder/chromium-ref/v8 and /home/coder/chromium-ref/src @ f288fed6)",
  "claims_checked": 54,
  "verified": [
    "common_includes.py line 2 = '# Copyright 2013 the V8 project authors.' — the 'Copyright 2013' attribution holds.",
    "common_includes.py:340 `def Run(self)`; :342 `state_file = \"%s-state.json\" % self._config[\"PERSISTFILE_BASENAME\"]`; :343-344 reload from JSON if the file exists; :348-351 `try: return self.RunStep()` / `finally:` / `TextToFile(json.dumps(self._state), state_file)`. Verified in the checkout.",
    "common_includes.py:741 `parser.add_argument(\"-s\", \"--step\", help=\"Specify the step where to start work. Default: 0.\", default=0, type=int)` — the `-s N` resume.",
    "common_includes.py:401-407 is `def Git(...)`, with `result = self.Retry(cmd, retry_on, [5, 30])` at :404. The [5, 30] wait-plan is exact. (Note: plain `Command` at :399 uses [5]; the section only claims [5, 30] for git, which is right.)",
    "common_includes.py:283-301 is `def Tag(...)`: `for wait_interval in [10, 30, 60, 60, 60, 60, 60]:` at :288, `self.step.GitLog(n=1, format=\"%H\", grep=message, branch=remote)` (fetch + grep for the unique commit title), and the `else:` clause dying with 'Couldn't determine commit for setting the tag. Maybe the git updater is lagging behind?' at :297-298. Quote verbatim.",
    "common_includes.py:487-498 `ReadAndPersistVersion` — `re.match(r\"^#define %s\\s+(\\d*)\" % def_name, line)` over the four names V8_MAJOR_VERSION / V8_MINOR_VERSION / V8_BUILD_NUMBER / V8_PATCH_LEVEL, reading VERSION_FILE = include/v8-version.h (defined at :52). 'grepped, not parsed' holds.",
    "include/v8-version.h at the reference checkout: MAJOR 15 (:11), MINOR 6 (:12), BUILD_NUMBER 0 (:13), PATCH_LEVEL 0 (:14), V8_IS_CANDIDATE_VERSION 1 (:18). '15.6.0 with V8_IS_CANDIDATE_VERSION 1' holds.",
    "create_release.py:41-57 — the <pre> excerpt matches the checkout byte for byte, including the two comments, `sorted([main_version, latest_version], key=LooseVersion)[1]`, `str(int(self[\"authoritative_build\"]) + 1)`, new_patch \"0\" and new_candidate \"0\".",
    "create_release.py `_Steps` returns exactly 13 classes: Preparation, PrepareBranchRevision, IncrementVersion, DetectLastRelease, DeleteBranchRef, PushBranchRef, MakeBranch, SetVersion, EnableMergeWatchlist, CommitBranch, LandBranch, TagRevision, CleanUp. Figure lane 2's 13 labels match this list in order.",
    "create_release.py:115-125 `class EnableMergeWatchlist` — `re.sub(r\"(# 'v8-merges@googlegroups\\.com',)\", \"'v8-merges@googlegroups.com',\", ...)` on WATCHLISTS_FILE. The un-comment claim holds at the cited lines.",
    "create_release.py:139-159 `class LandBranch` — GitUpload(force=True, bypass_hooks=True, no_autocc=True, set_bot_commit=True) at :146-150, then '# TODO(crbug.com/1176141): This might need to go through CQ.' at :151 and `cmd = \"cl land --bypass-hooks -f\"` at :153 run through self.Git. Both the flags and the TODO hold.",
    "create_release.py:122 PERSISTFILE_BASENAME = '/tmp/create-releases-tempfile', so the figure's '/tmp/create-releases-tempfile-state.json' is the correct state-file name given common_includes.py:342.",
    "auto_push.py:42-48 `class FetchCandidate` fetches '+refs/heads/lkgr:refs/heads/lkgr' and stores show-ref -s as the candidate; :51-61 `class LastReleaseBailout` diffs GetLatestReleaseBase()..candidate and returns True ('Already pushed current candidate') when empty. Cited range 42-61 is correct.",
    "merge_to_branch_gerrit.py:92-107 — `commit_msg[0] = f'Merged: {commit_msg[0]}'` (:101), the TAGS_TO_STRIP filter (:102-105), and `commit_msg.append(f\"(cherry picked from commit {original_commit['commit']})\")` (:106). TAGS_TO_STRIP at :22-29 = Change-Id, Reviewed-on, Reviewed-by, Commit-Queue, Cr-Commit-Position, Auto-Submit — so Change-Id / Reviewed-on / Commit-Queue are all genuinely in the list.",
    "roll_merge_gerrit.py:154-175 — `new_patch = version['patch'] + 1` (:154), the roll-merge hashtag POST with the comment \"# Add the 'roll_merge' hashtag so that Rubberstamper knows that this is a benign cherry pick.\" (:158-165), and the V8_PATCH_LEVEL re.sub + gerrit_util.ChangeEdit (:169-175). Cited range exact.",
    "Figure inset 'Owners-Override +1 · rubber-stamper' — roll_merge_gerrit.py:196-208 sets labels={\"Owners-Override\": 1} and adds rubber-stamper@appspot.gserviceaccount.com as reviewer.",
    "Figure inset 'submit · tag · wait for the -pgo tag, 15-20 min' — dossier release-scripts.json fact: roll_merge_gerrit.py:213-299, 'Waiting for PGO profile tag (%s-pgo), this can take 15-20 minutes...'. The caption's 'the number the script itself quotes' is accurate.",
    "detect_roll_candidate: gclient ['getdep', '--var=v8_revision', f'--deps-file={deps_file}'], 'for-each-ref', '--count=160', '--sort=-committerdate', and git push '+{next_v8_revision}:refs/heads/roll' — dossier fact (recipes/v8/detect_roll_candidate.py:78-97, 118-133, 149-159), verified-web.",
    "Three-hour priority window for patched versions: recipe_modules/v8/v8version.py:21 PATCH_VERSION_OFFSET_HOURS = 3, and choose_revision_to_roll only returns versions present in pgo_versions — dossier fact, verified-web. The section's '-pgo tag is the real gate' follows.",
    "v8-chromium-autoroll mode RUNNING, strategy BATCH set 2023-09-01, try_jobs list of 50 — autoroll.skia.org GetStatus fetched 2026-09-21, dossier fact. Caption already badges lane 4 as live status, not steady state.",
    "Figure caption 'refs/heads/15.6.31 pushed by v8-ci-autoroll-builder' — dossier fact: commit 'Version 15.6.31' on refs/heads/15.6.31, Bot-Commit: v8-ci-autoroll-builder@chops-service-accounts, Cr-Branched-From main@{#109971}.",
    "Figure lane 1 'V8 lkgr finder runs every 6 minutes' and 'recipe v8/auto_roll_push' — dossier release-scripts.json (auto_push.py executed by recipe v8/auto_roll_push as v8-ci-autoroll-builder; lkgr maintained every 6 minutes by the 'V8 lkgr finder' builder).",
    "build/util/version.py:59 is exactly `if official_build == '1':`, with LASTCHANGE at :60 and LASTCHANGE.dummy at :62. The section's 'picked by an if on line 59' is correct as written; cited span 44-66 contains the whole FetchValues body.",
    "version.py is a template engine over @KEY@ placeholders — SubstTemplate at :74-88, \"Keywords to be substituted are surrounded by '@': @KEYWORD@\" at :78.",
    "chrome/VERSION at f288fed6 = MAJOR=156 MINOR=0 BUILD=8067 PATCH=0, so '156.0.8067.0' holds.",
    "android_chrome_version.py:37-44 — the docstring 'Version code values are constructed like this:' with the positional format and the two worked examples (372100015, 372100920). :76 `_NEXT_BUILD_VERSION_CODE_DIFF = 50000`, with the preceding docstring explaining the total-ordering workaround. Both cited line numbers exact.",
    "build/util/lastchange.py:166-188 — the <pre> excerpt matches the checkout byte for byte; line 188 is `return VersionInfo(hash_, revision, pos, int(commit_timestamp))`, line 166 is `hash_ = ''`. The `cat-file commit` read and the `Cr-Commit-Position:` footer scan are at :182-187.",
    "build/util/lastchange.gni:5-16 — comment 'This file is used to inject fixed dummy commit for commit independent reproducible binaries.' (:5-6), `use_dummy_lastchange = !is_official_build` (:9), and the LASTCHANGE.dummy / LASTCHANGE branch (:12-16). Quote verbatim.",
    "build/util/LASTCHANGE.dummy = 'LASTCHANGE=' + 40 zeros + '-' + 40 zeros. Counted.",
    "chrome/tools/build/README.md:1-4 — \"'FILES.cfg' for each platform is now obsolete with respect to archiving (crbug.com/40201757).\"",
    "infra/archive_config/ contains exactly 11 .json files plus README.md (android-archive-rel, android-arm64-archive-rel, android-desktop-arm64-archive-rel, android-desktop-x64-archive-rel, linux-archive-rel, linux-chromiumos-full, mac-archive-rel, mac-arm64-archive-rel, win-archive-rel, win-arm64-archive-rel, win32-archive-rel). Count of 11 holds.",
    "create_installer_archive.py:85-99 — the comment at :85-92 contains verbatim 'This results in a ~2.3MB decrease in installer size on a 24MB installer.' and 'This results in an additional ~3.5MB decrease.'; '-m0=BCJ2' is at :93 and '-m1=LZMA:d27:fb128' at :94.",
    "chrome.7z: ARCHIVE_SUFFIX = '.7z' (:26) and --output_name default 'chrome' (:893-897); the module docstring (:8) says it reads chrome.release. Both details in the section hold.",
    "create_installer_archive.py:516-527 — the makecab.py comment ('Use makecab.py instead of makecab.exe so that this works when building on non-Windows hosts too') at :516-520 and `makecab_py = os.path.join(os.path.dirname(__file__), 'makecab.py')` at :521. The Linux-bot rationale is the tree's own.",
    "tools/clang/scripts/update.py:43-44 — CLANG_REVISION = 'llvmorg-24-init-7747-g62397f8b', CLANG_SUB_REVISION = 29. Both exact.",
    "upload_revision.py:48-59 — BUILD_CLANG_BOTS (linux/mac/mac_arm/win _upload_clang, 4 entries) and BUILD_RUST_BOTS (4 entries). 'four and four' holds.",
    "upload_revision.py:444-462 — `Git('cl', 'upload', '-f', '--bypass-hooks', '--squash', ...)` at :453 then two `git cl try -B chromium/try` blocks for the clang and rust bot lists.",
    "grep -c '^Cq-Include-Trybots:' upload_revision.py = 28. (The dossier's '~30' is superseded by the local count; the section's 28 is right.)",
    "tools/clang/scripts/package.py:140 — `['-h', 'x-goog-if-generation-match:0', 'cp']` with the comment 'Fail if the item generation is not 0 (i.e. if it already exists).' The '--upload' flag exists at :232.",
    "docs/updating_clang.md — the third numbered step is 'Run [go/chrome-promote-clang]... This will copy the specified packages from the untrusted staging bucket to the production bucket'; the sub-bullet reads 'Writing to the production bucket requires special permissions.' go/ link ⇒ Googler-only, as the section says.",
    "Recipe bundle latency 0.7-4.2 minutes across the 12 newest instances, computed 2026-09-22 — dossier gap-change-propagation fact 18 (registeredTs vs git_revision committer time; 3.0 min for 1bb808e3).",
    "`recipes.py fetch` → `recipes.py bundle --destination <dir>` → `cipd create -install-mode copy -tag git_revision:<sha> -ref <ref>` — dossier fact 16 (recipe_bundler/cmd_bundle.go:342-393).",
    "479 refs/heads/chromium/NNNN refs last modified 2020-11-16 (of 483 refs; master 2022-11-29, main 2026-09-21) — dossier fact 20, CIPD ListRefs 2026-09-22.",
    "Branch projects run trunk recipes — dossier fact 21 (M155 cr-buildbucket.cfg: 253 bootstrapper builders, every bundle pin at refs/heads/main while top_level_project.ref is refs/branch-heads/8059).",
    "Recipe autoroller 10-minute cadence, 163 CLs in 30 days to 2026-09-22, 122 MERGED / 41 ABANDONED — dossier fact 36 (Gerrit query, minute-of-hour histogram clustering at :x3).",
    "Bootstrapper is Go and packaged for exactly 7 platforms (linux-amd64, linux-arm64, mac-amd64, mac-arm64, windows-386, windows-amd64, windows-arm64) — dossier fact 76, build/packages/chromium-bootstrapper.yaml.",
    "'latest' carries 210 luci_build/git_revision tag pairs over 7.4 days on one unchanged instance because the build is reproducible — dossier facts 80-81; '-ref latest' added by build.py unless update_latest_ref is false (fact 79).",
    "bbagent pinned in Buildbucket service settings with a separate version_canary; extractCipdVersion returns VersionCanary only for canary builds — dossier fact 82 (service_config.proto:200-209, 235-238; schedule_build.go:1698-1702).",
    "src/infra/config/subprojects/chromium/try.star:23 — `task_template_canary_percentage = 5,` inside the try bucket's swarming defaults. Read in the local checkout this pass.",
    "Structure: exactly one <h2 id=\"the-release-scripts\"> carrying <span class=\"secnum\">{{S}}</span>; one <figure id=\"fig-v8-life\"> with figure-head / figure-body / figure-caption and label 'Figure {{N}}'; prose cross-reference uses 'Figure {{fig-v8-life}}', matching the convention in the other twelve sections; all three SVG marker ids prefixed fig-v8-life-; no <img>, <link>, <script> or external URL anywhere; all <, > and & inside <code>/<pre> escaped (checked programmatically)."
  ],
  "corrected": [
    "Lede: 'A DEPS hook writing one string into a header.' → '...into a file.' — src/DEPS:4562-4568 the 'lastchange' hook writes src/build/util/LASTCHANGE, a plain KEY=VALUE file; only the CrOS-only 'lastchange_commit_position_cros' hook writes a header (lastchange_commit_position.h).",
    "Step.Run citation [common_includes.py:340-352] → [:340-351] — line 352 is `def RunStep`; the Run method's last line (the finally: TextToFile) is 351.",
    "create_release.py step-list citation [:199-226] → [:211-226] — line 199 is `def _ProcessOptions`; `def _Steps` is at 211 and the list closes at 226.",
    "Android versionCode format made verbatim: '{full BUILD}{3 digits PATCH}{1 digit package}{1 digit ABI}' → '{full BUILD number}{3 digits: PATCH}{1 digit: package}{1 digit: ABIs}' — android_chrome_version.py:39, quoted exactly as the docstring writes it.",
    "'11 JSON files in infra/archive_config/ drive the archive builders instead' → 'drive the dev archive builders instead, the official ones being configured in src-internal' — chrome/tools/build/README.md:3-4 scopes the directory: 'New config files for archiving build artifacts are located in src/infra/archive_config for dev builders and in src-internal/testing/buildbot/archive for official builders.'",
    "Differential-installer citation [:431-452] → [:431-452, 241-258] — the bsdiff/Zucchini choice itself is in GenerateDiffPatch at 241-258 (`if options.diff_algorithm == \"ZUCCHINI\": ... ZUCCHINI_EXEC ... else: ... BSDIFF_EXEC`); 431-452 is only the call site.",
    "Quote from docs/updating_clang.md restored to its verbatim capitalisation: 'writing to the production bucket requires special permissions' → 'Writing to the production bucket requires special permissions'."
  ],
  "removed": [],
  "badged_reconstruction": [],
  "notes": [
    "The V8 lines are cited against /home/coder/chromium-ref/v8 (the v8 reference checkout), not chromium/src; every V8 line number in the section was re-derived by sed on that tree this pass.",
    "The dossier's clang fact says '~30 Cq-Include-Trybots lines'; the local grep gives exactly 28, which is what the section says. No change needed — the section is the more precise of the two.",
    "detect_roll_candidate.py, v8version.py, the Skia roller status, the CIPD/Gerrit measurements and the bbagent pin are web-sourced (not in any local checkout); each was checked against the dossier's evidence quote rather than a file, and the section's figure caption already labels lane 4 as live status fetched 2026-09-21.",
    "common_includes.py's generic Command() retries on [5]; only Git() uses [5, 30]. The section attributes [5, 30] to git calls specifically, which is correct."
  ],
  "skipped_existing": false
}
