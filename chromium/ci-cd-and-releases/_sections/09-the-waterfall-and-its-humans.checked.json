{
  "section_id": "09-the-waterfall-and-its-humans",
  "post": "ci-cd-and-releases",
  "checked_at": "2026-09-23",
  "reference": {
    "src": "/home/coder/chromium-ref/src @ f288fed6c601ae22461c9c9d8189004050abfaeb (2026-09-18)",
    "depot_tools": "/home/coder/chromium-ref/depot_tools",
    "web": "luci-go (notify.go, tree_status.go, createrevert.go, commitrevert.go) fetched from chromium.googlesource.com on 2026-09-23; chromium/src commit history via api.github.com"
  },
  "checked": 34,
  "verified": [
    "chromium-gitiles-trigger block at generated/luci/luci-scheduler.cfg:6647-7040 contains exactly 386 `triggers:` lines; gitiles refs are `regexp:refs/heads/main` on chromium/src (recounted with python).",
    "ci bucket holds 641 builders in generated/luci/cr-buildbucket.cfg (try 500, ci.shadow 1); 641 - 386 = 255 triggered by parents.",
    "trigger_child_builds emitting scheduler.GitilesTrigger with parent_got_* and deps_revision_overrides: DOSSIER facts LS49 / BU64 (tools/build not in the local checkout).",
    "133 `tree_closers {` and 252 `^notifiers {` in generated/luci/luci-notify.cfg; the 133 resolve to 133 distinct builders, all in bucket ci.",
    "notifiers.star:85-125 is exactly the quoted span: TREE_CLOSING_STEPS_REGEXP at :85, the seven entries, tree_closer() at :106, chromium-tree-closer at :116-120, close-on-any-step-failure at :122-125. Both name tree_status_host chromium-status.appspot.com; only the first carries failed_step_regexp.",
    "The <pre>'s editorial comment about branches matches notifiers.star:95-99 ('erroneous tree closure notifications won't be sent for failures on branches') and the _empty_notifier fallback at :100-104.",
    "luci-go notify.go: the SUCCESS/FAILURE guard and its comment sit inside UpdateTreeClosers, which starts at :392; the quote runs :399-404. CORRECTED the cited range.",
    "notify.go:316 compiles failed_step_regexp as fmt.Sprintf(\"^%s$\", ...) — the anchoring that makes the `runhooks` and `update` entries dead rather than redundant. Supports the badged reconstruction.",
    "depot_tools bot_update/api.py:310 is `kwargs.setdefault(\"infra_step\", True)` (the dossier's :309 is off by one; the section's :310 is right).",
    "depot_tools gclient/api.py:302-306 is def runhooks(...) calling self(name, [\"runhooks\"] + ..., infra_step=False); gclient/api.py:86-89 prefixes step names with \"gclient \", so the step is named `gclient runhooks`.",
    "--nohooks --noprehooks are at bot_update/resources/bot_update.py:459-460, not api.py; added the path.",
    "tree_status.go:262-276 — 'Manually Closed | Always leave unchanged'; automatically closed reopens when all tree-closer builds pass or all failing builds started after the last automated revert / manual reopen; throttled and maintenance interpreted as closed.",
    "chromium-status.appspot.com as a facade: status.py:39-40 TREE_STATUS_SERVICE, and the 2026-09-22 readings (createTime 2026-09-20T12:19:05.626463Z, 'Tree is open (Automatic: All tree-closer builders are passing)') — DOSSIER LS93 / V11, verification pass R10.",
    "gardener_rotations.star:10-23 declares exactly 12 rotations (ANDROID, ANGLE, CHROMIUM, CFT, DAWN, FUCHSIA, CHROMIUM_CLANG, CHROMIUM_GPU, IOS, CHROMIUMOS, CRONET, CROSSBENCH).",
    "generated/sheriff-rotations/ holds 11 .txt files, 436 lines total; chromium 141, chromium.gpu 94, dawn 56, chromium.clang 46, android 43 (chromiumos has no generated list — the 12th rotation).",
    "docs/sheriff.md is a 5-line redirect stub to gardener.md.",
    "docs/gardener.md:3-35 carries the priority list; '**The tree is open**, because when the tree is closed nobody can make progress' is verbatim, as are the Owners-Override and Rubber Stamper authorities.",
    "docs/gardener.md:62 carries 'if >8 hours have passed, that person is no longer gardener' verbatim (escaped as &gt; in the section).",
    "Sheriffs -> gardeners rename announced 2023-05-17 (Erik Staab, chromium-dev): DOSSIER CP94.",
    "Owners-Override+1 restricted to gardeners/release PMs and Rubber Stamper's Bot-Commit+1 for clean reverts: DOSSIER CP37 / CP38 (docs/code_reviews.md:235-247, :298-313).",
    "'formerly GoFindit ... the rewrite in Golang of the Python2 version of Findit' is verbatim from luci-go bisection/README.md: DOSSIER LS96.",
    "infra/config/luci-bisection.cfg:4-24 matches the <pre> verbatim, line for line, ending at max_revertible_culprit_age: 21600 on :24.",
    "luci-bisection.cfg is hand-written and byte-identical to generated/luci/luci-bisection.cfg (cmp reports no difference) — 'only copied by lucicfg' holds.",
    "test_analysis_config points at findit/test-single-revision with create_revert enabled/daily_limit 10 and submit_revert enabled: false — 'creates reverts it never submits'.",
    "createrevert.go refusal strings verified verbatim by fetching the file: sub-commit in a rolled dependency (:40), 'not watched by gardeners, therefore less important' (:51, :66), 'auto-revert has been disabled for this CL by its description' (:76), 'LUCI Bisection cannot revert changes from this CL's author' (:85), 'there are merged changes depending on it' (:94); the NOAUTOREVERT regexp is DOSSIER CP107.",
    "commitrevert.go:39-52 — the test-failure auto-commit refusal (:39) and MaxRevertibleCulpritAge (:47-52). This is where the 21,600 s gate lives; it is a submit-time check, not a create-time one.",
    "tree_closure_email.template quotes 'GardenerAI is currently attempting to identify and revert the culprit. Please defer investigation until you receive a IRM notification.' verbatim; the hand-written source path is cited (per DOSSIER LS92, the generated copy is byte-identical).",
    "The GardenerAI sentence landed in commit 17e3f5e466, 2026-02-06 ('[Gardener AI] instruction in notification template to tell gardener wait until IRM to take action'); the template itself dates from 2020-08-05. Confirms 'a 2026 layer'.",
    "chromium.perf.json: 73 keys minus 2 AAAAA sentinels = 71 entries; 18 contain '-builder-perf', 7 end '-processor-perf', 46 testers remain; swarming.shards summed over the 46 testers = 628.",
    "docs/speed/perf_lab_platforms.md 'Primary Platforms' (:7-59) has exactly 39 bullets.",
    "Pinpoint /api/isolate POST of each isolate's CAS digest: DOSSIER PF60 (tools/build perf_dashboard/api.py).",
    "docs/speed/perf_waterfall.md:17-42 is the 'How It Works' section; 'a single tester job running continuously' and 'the set of commits from the end commit of the previous invocation to the latest _built_ commit' are both verbatim.",
    "No CQ verifier in generated/luci/commit-queue.cfg has 'perf' in its name — 'no perf tester is a required CQ builder' holds.",
    "android-binary-size (16kb arm32 / 64kb high-end arm64, android_binary_size_trybot.md:29-30 and :100-104 for 50 dex methods), compile-size (tryserver.chromium.linux.star:90 \"size_threshold_mib\": 200) and fuchsia-binary-size all carry '\"cq\": \"required\"' in cr-buildbucket.cfg and appear in commit-queue.cfg without includable_only or experiment_percentage; Binary-Size: and Compile-Size: footers exist (android_binary_size_trybot.md:87-89, compile_size_builder.md:121-128)."
  ],
  "corrected": [
    "notify.go citation :302-339 -> :392-404. Fetched luci_notify/notify/notify.go from chromium.googlesource.com: UpdateTreeClosers starts at :392 and the quoted comment plus its SUCCESS/FAILURE guard run :399-404. The dossier's BU68 source field lists :302-339,392-426, but its own code excerpt X113 and the live file put the quote in the second range.",
    "--nohooks --noprehooks attributed to bot_update generally -> cited as depot_tools bot_update/resources/bot_update.py:459-460. The flags are not in bot_update/api.py; grep found them only in the resource script.",
    "Figure, LUCI Bisection refusal box: '· the culprit is older than 21,600 s' -> '· the culprit is a sub-commit of a rolled dep'. The age gate is not a create-time refusal — MaxRevertibleCulpritAge is read in commitrevert.go:47-52, while createrevert.go:40 refuses sub-commits inside a rolled dependency. Source: the two luci-go files, fetched 2026-09-23.",
    "Figure, submit box third line: 'Commit-Queue+2 · compile failures only' -> 'Commit-Queue+2 · compile only · culprit under 6 h', so the age gate now sits where the code applies it (commitrevert.go:39-52).",
    "Prose: 'Ten reverts created a day, four submitted, only for a culprit younger than 21,600 seconds' -> the age is bound explicitly to submission, with the commitrevert.go:39-52 citation and the test-failure auto-commit refusal. Source: luci-go commitrevert.go:39-52.",
    "Prose refusal list: added the fifth createrevert.go refusal, 'a sub-commit inside a rolled dependency' (verbatim from createrevert.go:40), so prose and figure list the same five gates.",
    "GardenerAI: 'A 2026 layer ... visible only as one sentence' -> 'added on 6 February 2026 and visible only as a note in one e-mail template'. The passage already quotes two sentences and the template carries a third GardenerAI sentence ('An IRM alert will be triggered if the GenAI tool fails to resolve the closure'). The date is from commit 17e3f5e466 (api.github.com, path infra/config/templates/tree_closure_email.template).",
    "'No public design doc exists; that quote is the whole public surface.' -> 'I found no public design doc for it; that note is the whole public surface' with a <span class=\"recon\">reconstruction</span> badge. An unverifiable universal negative became a scoped first-person claim, per the house rule on unbadged specifics.",
    "Perf glob '18 <code>*-builder-perf</code>' -> '18 <code>*-builder-perf*</code>'. Recounted in testing/buildbot/chromium.perf.json: only 11 keys end exactly in -builder-perf; the 18 include -builder-perf-pgo and linux-builder-perf-rel. The 18/46/7 split and the 71 total are correct."
  ],
  "removed": [],
  "structure": {
    "h2": "one <h2 id=\"the-waterfall-and-its-humans\"> with <span class=\"secnum\">{{S}}</span>",
    "figures": "one figure, id fig-tree-closure, with figure-head / figure-body / figure-caption and label 'Figure {{N}}'; cross-reference written 'Figure {{fig-tree-closure}}' per house convention",
    "svg_marker_ids": "fig-tree-closure-arr-ink, fig-tree-closure-arr-purple, fig-tree-closure-arr-red — all prefixed by the figure id",
    "external_resources": "none: no <img>, <link>, <script>, and no http(s) URL anywhere in the body",
    "escaping": "all < > & inside <code>/<pre> are escaped (&gt;8 hours, &mdash;, &ndash;); leaf-level scan clean"
  },
  "notes": [
    "The section's central reconstruction — that bot_update, runhooks and update are dead entries in TREE_CLOSING_STEPS_REGEXP — got stronger, not weaker: notify.go:316 compiles failed_step_regexp as ^...$, so `runhooks` cannot match the step actually named 'gclient runhooks'. The claim is still badged as reconstruction because it rests on a negative over tools/build, which is not in the checkout.",
    "The figure note 'So the purple row is dead config, not a rule' calls the bot_update row purple while the row is drawn red and the legend reserves purple for state arrows. Left as written — 'purple' is the LUCI status colour for INFRA_FAILURE and the prose sets it up ('ends the build purple, as INFRA_FAILURE') — but flagged for the editor pass.",
    "The dossier's own scheduler citation (luci-scheduler.cfg:6647-7039) is one line short of the block; the section's :6647-7040 is correct."
  ]
}
