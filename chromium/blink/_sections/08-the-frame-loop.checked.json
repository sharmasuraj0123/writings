{
  "section_id": "08-the-frame-loop",
  "checked_at": "2026-09-22",
  "checkout": "chromium/src @ f288fed6 (2026-09-18, sparse). cc/, third_party/blink/renderer/bindings/, third_party/blink/public/ and runtime_enabled_features.json5 are in the sparse checkout and were read directly. core/frame/, core/page/, core/execution_context/, core/timing/ and platform/scheduler/ are not in it, so those files were fetched from https://raw.githubusercontent.com/chromium/chromium/main/... on 2026-09-22 and read directly; v8/include/v8-microtask-queue.h from /home/coder/chromium-ref/v8. Web-sourced claims (HTML spec step count, Jake Archibald 2015, CL/commit dates) were checked against the verbatim evidence quotes in chromium/_dossiers/blink/DOSSIER.md.",
  "checked": 78,
  "corrected": [
    "Figure caption: \"The spec numbers are the code's own annotations where the source writes them — 6 through 14 in core/page/page_animator.cc, 16 and 21 in core/frame/local_frame_view.cc, 19 named in the comment above LayerTreeHost::DidBeginMainFrame; the 22 under commit is mine\" -> \"Two of the spec numbers are the code's own annotations — 6 through 12 and 14 in core/page/page_animator.cc:205-351, and 21 in core/frame/local_frame_view.cc:2715 (// 21. For each doc of docs, mark paint timing for doc.) from the tree. The 16 over the lifecycle loop, the 19 over post-lifecycle IntersectionObserver delivery and the 22 under commit are mine, read off the spec's step list [reconstruction]\". Two provenance claims were false. (a) local_frame_view.cc annotates exactly one step number: `grep -n \"// *[0-9]\\{1,2\\}\\. \" local_frame_view.cc` returns only line 2715, so 16 is not written in that file. (b) The comment above LayerTreeHost::DidBeginMainFrame is cc/trees/proxy_main.cc:568-573, read locally: \"For Blink implementations, this is the typical hook that will deliver intersection observer events for chromium-internal customers, see: blink::LocalFrameView::RunPostLifecycleSteps.\" — it names intersection observer delivery, never the number 19. The dossier's own numbers row agrees: \"Blink annotates 6-12, 14 and 21 by number\" (DOSSIER.md:5966); 19 is the spec's number for \"update intersection observations\" (EL-F2) and EL-F79 files RunPostLifecycleSteps only as \"spec steps 19/22 territory\". Moved 16, 19 and 22 under the existing reconstruction badge and left 6-12/14 and 21 badged from the tree.",
    "\"with exactly one hole: ResizeObserver callbacks are wrapped in ScriptForbiddenScope::AllowUserAgentScript. That is the only place author script runs inside the lifecycle.\" -> \"with one hole that ships: ResizeObserver callbacks are wrapped in ScriptForbiddenScope::AllowUserAgentScript (:2650-2661). A second such wrapper, around the container-query change steps (:2679), is gated on ElementMatchContainer, whose status in runtime_enabled_features.json5 is test — off in a release build [from the tree]. In shipping Chrome, ResizeObserver is the only place author script runs inside the lifecycle.\" — UpdateLifecyclePhasesInternal (main core/frame/local_frame_view.cc:2515-2758, fetched 2026-09-22) contains two ScriptForbiddenScope::AllowUserAgentScript blocks, not one: :2651 around RunResizeObserverSteps and :2679 around RunContainerQueryListSteps. The second is real author script (ContainerQueryListController::NotifyChanges) but LocalFrameView::RunContainerQueryListSteps:2857-2860 returns false unless RuntimeEnabledFeatures::ElementMatchContainerEnabled(), and third_party/blink/renderer/platform/runtime_enabled_features.json5:2673-2674 in the local checkout reads name: \"ElementMatchContainer\", status: \"test\" — so the claim holds only for shipping builds. The dossier had the same simplification (DOSSIER.md:86, \"the one place author script runs inside the lifecycle\"), while EL-F73 does list \"container-query list steps\" inside the loop.",
    "\"core/frame/local_frame_view.cc:4039-4047\" -> \"core/frame/local_frame_view.cc:4040-4048\" for LocalFrameView::ScheduleAnimation. Read from the chromium/main mirror on 2026-09-22: 4039 is blank, the signature starts at 4040 (void LocalFrameView::ScheduleAnimation(cc::BeginMainFrameReason reason, ...)), client->ScheduleAnimation is 4047 and the closing brace is 4048. The dossier's 4039-4047 (DOSSIER.md:86, fetched 2026-09-21) is one line earlier — a one-line shift in the file between the two fetches, or an off-by-one; the range verified today is used.",
    "Code-block lead-in \"cc/trees/proxy_main.cc:354-377, comments trimmed\" -> \"...:354-377, comments and one UI-resource branch trimmed\" — the excerpt is verbatim against the local checkout for lines 354, 356-358, 362, 367 and 375-377, but it also silently drops the whole statement at proxy_main.cc:369-373 (the comment plus `if (begin_main_frame_state->evicted_ui_resources) layer_tree_host_->GetUIResourceManager()->RecreateUIResources();`), which is not a comment. The elision is now declared."
  ],
  "removed": [],
  "verified_unchanged": [
    "event_loop.h:40-46 — the seven-line <pre> is exact, character for character, against https://raw.githubusercontent.com/chromium/chromium/main/third_party/blink/renderer/platform/scheduler/public/event_loop.h fetched 2026-09-22 (line 40 \"// The specification says an event loop has (non-micro) task queues. However,\" through line 46 \"// queue: <https://html.spec.whatwg.org/C#microtask-queue>\"). The section's own statement that the file is absent from the sparse checkout and was read from the main mirror is true: `ls` on the checkout path fails.",
    "core/execution_context/window_agent.cc:12-16 — fetched from main 2026-09-22: line 11 is the WindowAgent constructor signature, 12-16 the initializer list, with v8::MicrotaskQueue::New(agent_group_scheduler.Isolate(), v8::MicrotasksPolicy::kScoped) on 15-16. Matches ledger rows V10 and EL-F12.",
    "bindings/core/v8/local_window_proxy.cc:349-352 — read locally at f288fed6: `context = v8::Context::New(isolate, nullptr, global_template, global_proxy, v8::DeserializeInternalFieldsCallback(), GetFrame()->DomWindow()->GetMicrotaskQueue());`. The FromSnapshot path is the branch above it, V8ContextSnapshot::CreateContextFromSnapshot at :336-337.",
    "bindings/core/v8/v8_initializer.cc:889 — read locally: `isolate->SetMicrotasksPolicy(v8::MicrotasksPolicy::kScoped);` inside V8Initializer::InitializeV8Common (:884), so \"set on every isolate Blink creates\" holds.",
    "\"kScoped is set twice over\" — v8/include/v8-microtask-queue.h:49-50 read locally: `static MicrotaskQueue* New(Isolate* isolate, MicrotasksPolicy policy = MicrotasksPolicy::kAuto);`, so the per-agent kScoped argument is load-bearing. Ledger row V10.",
    "main_thread_scheduler_impl.cc:2621-2624 — fetched from main 2026-09-22: OnTaskCompleted opens at 2616, 2621 is the TRACE_EVENT, 2623 is the verbatim comment `// Microtasks may detach the task queue and invalidate |queue|.` and 2624 is `PerformMicrotaskCheckpoint();`. Nothing but the trace event precedes it, so \"before it does anything else\" is right. The section's cite is correct; the dossier's :2611-2619 (DOSSIER.md:86) is the stale one.",
    "Per-agent queues: landed 2022-10-05, CL 3928076, shipped M108, kill switch removed 2023-08-16 (CL 4780238) — EL-F13, evidence from the GitHub commit API for c1fb77438b37 and 1f8ed4479f86, plus timeline rows 6203/6204 and numbers row 5968.",
    "TaskType::kMainThreadTaskQueueCompositor = 38 — read locally at third_party/blink/public/platform/task_type.h:309.",
    "Priority ladder kVeryHigh / kExtremelyHigh / kHighest with the 100 ms and 500 ms thresholds — EL-F45 and numbers row 5969, quoting kDefaultRenderingStarvationThreshold = base::Milliseconds(100) and kRenderBlockingStarvationThreshold = base::Milliseconds(500) from main_thread_scheduler_impl.h:118-125.",
    "cc/scheduler/scheduler_state_machine.cc:505-552 — read locally: CouldSendBeginMainFrame (:505) rejects !visible_ (:510), begin_frame_source_paused_ (:515), defer_begin_main_frame_ (:519) and pause_rendering_ (:523); ShouldSendBeginMainFrame (:534) rejects a second frame in the same begin frame (:539) and BeginMainFrameState::SENT (:551-552). The four gates in the prose match.",
    "cc/trees/proxy_impl.cc:806-851 — read locally: ScheduledActionSendBeginMainFrame at :806, commit_data from ProcessCompositorDeltas at :817, mutator_events from TakeMutatorEvents at :825, and MainThreadTaskRunner()->PostTask(FROM_HERE, base::BindOnce(&ProxyMain::BeginMainFrame, ...)) at :848-851.",
    "cc/trees/proxy_main.cc:341-377 as the figure's band order — read locally: ApplyCompositorChanges :346, ApplyMutatorEvents :351, WillBeginMainFrame :354, BeginMainFrame :358, AnimateLayers :362, RequestMainFrameUpdate :377; the later bands (UpdateLayers, the blocking commit at :555-565, DidBeginMainFrame :574) are EL-F47 and EL-F48.",
    "core/page/page_animator.cc:205-351 — fetched from main 2026-09-22 and read: :205 is the spec URL, then the numbered comments 6 (:207), 7 (:251), 8 (:259), 9 (:266), 10 (:277), 11 (:292), 12 (:325), the unnumbered rVFC block (:329-333) and 14 (:335), ending at :351. Every step body is a run_for_all_*_controllers_with_timing loop, which is exactly \"each step for all documents before the next\".",
    "The 2023-02-27 rewrite — EL-F56: CL 4234962, commit 766274f6af98, quoting \"now each step is run for all docs at one time vs. it was all steps for one doc at one time\".",
    "AnimationClock frozen at frame time, coarsened to 100 µs / 5 µs when cross-origin isolated — EL-F58, and core/timing/time_clamper.h:23-24 fetched 2026-09-22: kCoarseResolutionMicroseconds = 100, kFineResolutionMicroseconds = 5.",
    "core/frame/local_frame_view.cc:2515-2563, 2650-2668, 2715-2718 — fetched from main 2026-09-22: UpdateLifecyclePhasesInternal opens at 2515 with BlinkLifecycleScopeWillBeScriptForbidden at :2520; RunStyleAndLayoutLifecyclePhases at :2560; the ResizeObserver block is :2650-2668; :2715-2718 is `// 21. For each doc of docs, mark paint timing for doc.` over ForAllNonThrottledLocalFrameViews(... MarkPaintTiming()). RunPaintLifecyclePhase follows at :2755, so \"then paint runs\" is right.",
    "23 top-level steps in the spec's update-the-rendering task as of the 2026-09-21 Living Standard — EL-F2 and numbers row 5966, step-counted from html.spec.whatwg.org/multipage/webappapis.html#update-the-rendering.",
    "The microtask-timing claim and its provenance — EL-F28 (corrected C03): the HTML spec's note under \"clean up after running script\" plus Jake Archibald 2015 (\"`.click()` causes the event to dispatch synchronously ... we don't process the microtask queue between listener callbacks\"), which is why the section says this is not an inference from the code.",
    "The three caveats — V8RunMicrotasksScope is a Blink wrapper landed 2026-03-03, commit 1be1a11d01, degrading to kDoNotRunMicrotasks while the EventLoop has microtasks paused (EL-F28); bfcache is such a pause (EL-F22, kBackForwardCachePauseMicrotasks, commit 117d86bef4eb); ~MicrotasksScope skips the drain on a pending exception (EL-F28, v8/main src/api/api.cc:11312-11322).",
    "rAF-aligned input flushed before Page::Animate — EL-F50 (widget_base.cc:1159-1185, DispatchRafAlignedInput(args.frame_time) then client_->BeginMainFrame(args)); RequestMainFrameUpdate -> UpdateLifecycle(kAll) -> UpdateAllLifecyclePhases — EL-F52, EL-F53.",
    "Figure \"runs script?\" row: JavaScript IntersectionObservers post a task rather than deliver in place — EL-F80, quoting intersection_observer_controller.cc \"Javascript observers will PostTask to invoke their callbacks.\"",
    "Figure aria-label: eleven bands named, matching the eleven <rect> bands drawn; \"TASKTYPE 38\" matches task_type.h:309."
  ],
  "structure": {
    "h2": "exactly one, <h2 id=\"the-frame-loop\"><span class=\"secnum\">{{S}}</span>One frame, unrolled</h2>",
    "figure_labels": "Figure {{N}} (one figure); the in-prose cross-reference is Figure {{fig-begin-main-frame}}, matching the convention in sections 01, 03, 04 and 05",
    "figures": "fig-begin-main-frame has id, figure-head, figure-body, figure-caption",
    "svg_marker_ids": "fig-begin-main-frame-arr-ink — prefixed by the figure id",
    "escaping": "all <, > and & escaped inside <code> and <pre> (checked by script over every innermost <code> body; the two <pre> blocks use &lt;, &gt; and -&gt;)",
    "external_resources": "no <img>, <link> or <script>",
    "tag_balance": "balanced (html.parser walk, no unclosed tags, no stray end tags)"
  },
  "skipped_existing": false
}
