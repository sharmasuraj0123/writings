{
  "section_id": "04-processes-threads-and-the-scheduler",
  "checked_at": "2026-09-22",
  "checkout": "chromium/src @ f288fed6c601ae22461c9c9d8189004050abfaeb (2026-09-18, sparse); files outside the sparse checkout fetched from raw.githubusercontent.com/chromium/chromium/main on 2026-09-22",
  "checked": 44,
  "corrected": [
    "TaskSchedulingInBlink.md:141 -> :142 — the stale sentence \"On mobile all pages are frozen after five minutes in the background.\" is at line 142; line 141 is blank. Verified in third_party/blink/renderer/platform/scheduler/TaskSchedulingInBlink.md at f288fed6 (byte-identical to chromium/main).",
    "page_scheduler_impl.cc \"same file, :851-852\" -> \":850-851\" — the branch gated on blink::features::kStopInBackground is `} else if (base::FeatureList::IsEnabled(` at :850 and `blink::features::kStopInBackground)) {` at :851; :852 is the unrelated kProactive check. Verified against third_party/blink/renderer/platform/scheduler/main_thread/page_scheduler_impl.cc (chromium/main).",
    "main_thread_scheduler_impl.h:118 -> :118-119 — kDefaultRenderingStarvationThreshold is declared at :118 and its base::Milliseconds(100) value is at :119, matching the :124-125 form used for kRenderBlockingStarvationThreshold. Verified against chromium/main.",
    "agent.h \":30-37, :57\" -> \":30-37, :52, :57\" — :57 is the event_loop() accessor and :30-37 the class comment/declaration, but the microtask queue enters at the constructor `v8::MicrotaskQueue* microtask_queue = nullptr` on :52 (member event_loop_ at :109). Verified against third_party/blink/renderer/core/execution_context/agent.h (chromium/main)."
  ],
  "removed": [],
  "verified": [
    "task_type.h @ f288fed6: grep -cE '^\\s+k[A-Za-z0-9_]+ = [0-9]+,' = 79; \"Next value: 88\" comment at :21; kInternalAutofill = 88 (:292) and kBackForwardCachePostedMessage = 89 (:296) — claim stands.",
    "task_priority.h:14-31 (chromium/main): the quoted <pre> block matches character for character, including kDefaultPriority = kNormalPriority and kPriorityCount = 11; 11 rungs, 0..10.",
    "web_frame.h:60-62 @ f288fed6: \"A remote frame is always cross-site; a local frame may be either same-site or cross-site.\" verbatim.",
    "web_local_frame.h:275-279 @ f288fed6: \"The frame must be a LocalRoot.\" in the InitializeFrameWidget comment.",
    "process_model_and_site_isolation.md @ f288fed6: Security Principal bullet at :80, scheme-plus-eTLD+1 wording at :86-87; \"roughly corresponds to the agent cluster ... although they do not match exactly\" at :106-108; Browsing Context Group bullet :120-131; same-principal/same-group rule at :98-100. Cited range :80-131 correct.",
    "process_model_and_site_isolation.md:141-195: Full Site Isolation = desktop (:141-155), Partial = Chrome for Android 2+ GB RAM (:156-178), No Site Isolation = <2 GB Android, WebView, iOS (:179-196). Chrome 67 / Chrome 77 and 10-13% / 3-5% are not in the md; they come from the dossier's verified-web facts PT-F7 and PT-F8 quoting chromium.org/Home/chromium-security/site-isolation/ — the section already attributes them to that page.",
    "process_model_and_site_isolation.md:453-459 @ f288fed6: the Sandboxed iframes bullet starts at :453 and \"Since 127.0.6483.0, Desktop Chromium moves these documents into a separate process from their parent or opener\" is at :455.",
    "renderer_main.cc (chromium/main): base::PlatformThread::SetName(\"CrRendererMain\") at :202; WebThreadScheduler::CreateMainThreadScheduler at :219-221; platform.EnableSandbox() at :255. Scheduler precedes sandbox.",
    "render_thread_impl.cc:839-840 (chromium/main): `if (!command_line.HasSwitch(switches::kDisableThreadedCompositing))` / `InitializeCompositorThread();`, inside RenderThreadImpl::InitializeWebKit which opens at :810 — the section's attribution to InitializeWebKit is correct.",
    "scheduler/common/thread.cc:76-80 (chromium/main): Thread::CreateAndSetCompositorThread, ThreadCreationParams(ThreadType::kCompositorThread) at :79 and base::ThreadType::kPresentation at :80; :98-107 is the IS_LINUX||IS_CHROMEOS block with \"This is not possible inside the sandbox, so ask the browser to do it.\" at :103. CompositorThread derives from NonMainThreadImpl (worker/compositor_thread.h:14).",
    "worker_backing_thread.h:22-25 (chromium/main): \"WorkerBackingThread represents a WebThread with Oilpan and V8.\"",
    "worker_thread.cc:84 (chromium/main): constexpr base::TimeDelta kForcibleTerminationDelay = base::Seconds(2);",
    "PausingAndFreezing.md:56-57: \"Only the none pausable tasks will execute in the nested event loop.\" — typo confirmed in tree. NOTE: the file lives at core/execution_context/PausingAndFreezing.md, not under platform/scheduler/; the section cites the bare filename, so no path is wrong.",
    "TaskSchedulingInBlink.md:31-34: \"if a task has started, it can't be interrupted until it completes. The scheduler can only choose a new task to run from the eligible tasks or can elect not to run any task at all.\" verbatim (note the source uses a typographic apostrophe).",
    "TaskSchedulingInBlink.md:38-47: FrameScheduler::GetTaskRunner and the freeze/prioritise rationale; :69-77: heading, \"Because tasks posted to it lack any attribution\" (:72-73) and \"usages are **banned** in `blink/` and `content/renderer/` directories\" (:75-76).",
    "frame_scheduler_impl.cc:487-668 (chromium/main): CreateQueueTraitsForTaskType opens at :487 and closes at :668 = 182 lines. kJavascriptTimerDelayedLowNesting/HighNesting -> ThrottleableTaskQueueTraits (:494-504); kJavascriptTimerImmediate -> DeferrableTaskQueueTraits with the comment \"Immediate timers are not throttled\" (:505-507).",
    "frame_scheduler_impl.cc:683-689: GetTaskQueue hands the computed QueueTraits to frame_task_queue_controller_->GetTaskQueue(queue_traits) — supports the figure note \"one queue per distinct trait set\".",
    "main_thread/use_case.h:16-53 (chromium/main; the file is under main_thread/, which is what the section cites): nine enumerators kNone, kEarlyLoading, kLoading, kTouchstart, kCompositorGesture, kSynchronizedGesture, kMainThreadGesture, kMainThreadCustomInputHandling, kDiscreteInputResponse, with kMaxValue at :53.",
    "user_model.h:21-39 (chromium/main): kGestureEstimationLimit 100 ms, kMedianGestureDuration 300 ms with \"This is based on two weeks of Android usage data.\", kExpectSubsequentGestureDeadline 2000 ms, kDiscreteInputResponseDeadline 50 ms.",
    "main_thread_scheduler_impl.cc:2915-2943: ComputeCompositorPriorityFromUseCase returns kLowPriority for kCompositorGesture (:2924) and kHighestPriority for kMainThreadGesture/kTouchstart/kDiscreteInputResponse (:2943).",
    "main_thread_scheduler_impl.cc:2690-2691: PrioritisationType::kInput -> kHighestPriority unconditionally; :2681-2684: the kDefault queue -> kVeryHighPriority when should_prioritize_ipc_tasks, else kNormalPriority — both figure arrows check out.",
    "main_thread_scheduler_impl.h:551: static const int kEndIdleWhenHiddenDelayMillis = 10000.",
    "idle_helper.h:30 \"Both idle queues are run a BEST_EFFORT priority\"; :36-41 \"typically less than 10ms\" / \"typically up to 50ms\"; :71-73 kMinimumIdlePeriodDuration = base::Milliseconds(1).",
    "page_scheduler_impl.cc:55-56: kDefaultDelayForBackgroundTabFreezing = base::Minutes(1); :49-50 kThrottlingDelayAfterBackgrounding = base::Seconds(10); PageSchedulerImpl::SetPageFrozen at :226.",
    "page_scheduler_impl.h:51-52 kDefaultThrottledWakeUpInterval = base::Seconds(1) and :55 kIntensiveThrottledWakeUpInterval = base::Minutes(1) — the figure timeline's \"wake-ups every 1 s\" and \"1 wake-up / minute\".",
    "blink/common/features.cc:2251-2261: BASE_FEATURE(kStopInBackground, \"stop-in-background\", #if BUILDFLAG(IS_ANDROID) && !BUILDFLAG(IS_CAST_ANDROID) && !BUILDFLAG(IS_DESKTOP_ANDROID) FEATURE_ENABLED_BY_DEFAULT #else FEATURE_DISABLED_BY_DEFAULT #endif).",
    "scheduler/common/features.h:40-41: kIntensiveWakeUpThrottling_GracePeriodSeconds_Default = 5 * 60 and ..._GracePeriodSecondsLoaded_Default = 60.",
    "\"true from 2019 until M143\": dossier fact V2 (verified-web) — kDefaultDelayForBackgroundTabFreezing was base::Minutes(5) at every release tag 75.0.3770.0 through 142.0.7444.0 and became base::Minutes(1) at 143.0.7499.0 via commit fffc79af056c (2025-10-06, CL 7013707); TaskSchedulingInBlink.md dates from 2019-01-21.",
    "page.h:112-115 (chromium/main): \"A Page roughly corresponds to a tab or popup window in a browser. It owns a tree of frames (a blink::FrameTree).\"",
    "local_dom_window.h:115-116 (chromium/main): class CORE_EXPORT LocalDOMWindow final : public DOMWindow, public ExecutionContext, ...",
    "core/execution_context/README.md:10 @ f288fed6: \"...however, it's inherited by Document instead of DOMWindow/LocalDOMWindow for historical reasons\" — still present, so the section's \"stale\" call stands.",
    "window_agent_factory.h:22-24 (chromium/main): \"The instance should be created for each group of Documents that are mutually reachable via `window.opener`, `window.frames` or others.\"",
    "Figure fig-containers labels: one Viz process for all of Chromium (dossier PT-F21/PL-F6, developer.chrome.com RenderingNG architecture); IO thread receives all IPC (PT-F18, docs/threading_and_tasks.md:136-153); AgentSchedulingGroup owns a WebAgentGroupScheduler (PT-F106); only a local root owns a widget and so a cc::LayerTreeHost (PT-F87, PT-F88); one isolate per main thread in shipping builds (PT-F41).",
    "Figure caption \"kMBIMode is off by default\": content/public/common/content_features.cc:711-717 — BASE_FEATURE(kMBIMode, ...) is FEATURE_ENABLED_BY_DEFAULT only under BUILDFLAG(MBI_MODE_PER_RENDER_PROCESS_HOST) || BUILDFLAG(MBI_MODE_PER_SITE_INSTANCE), otherwise FEATURE_DISABLED_BY_DEFAULT; kMBIModeParam defaults to MBIMode::kLegacy.",
    "Figure fig-scheduler hierarchy labels: PageScheduler's visible/frozen/bfcached/audible states are SetPageVisible (:41), SetPageFrozen (:52), SetPageBackForwardCached (:54), AudioStateChanged/IsAudioPlaying (:66, :68) in scheduler/public/page_scheduler.h; AgentGroupScheduler's independent default + compositor runners and Isolate() from web_agent_group_scheduler.h:22-48 (PT-F67)."
  ],
  "structural": {
    "h2": "exactly one <h2 id=\"processes-threads-and-the-scheduler\"> carrying <span class=\"secnum\">{{S}}</span>",
    "figure_labels": "2 figures, both <span class=\"figure-label\">Figure {{N}}</span>",
    "figure_parts": "both figures have id, figure-head, figure-body, figure-caption",
    "svg_markers": "fig-containers-arr-ink, fig-containers-arr-purple, fig-scheduler-arr-ink — all prefixed by their figure id",
    "escaping": "no raw <, > or bare & inside any <code> or <pre><code>",
    "external_resources": "no <img>, <link> or <script>"
  },
  "skipped_existing": false
}
