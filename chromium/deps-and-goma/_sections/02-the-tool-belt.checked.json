{
  "section_id": "02-the-tool-belt",
  "post": "deps-and-goma",
  "checked_at": "2026-09-23",
  "refs": {
    "src": "chromium/src@f288fed6c601ae22461c9c9d8189004050abfaeb (2026-09-18)",
    "depot_tools": "depot_tools@0306e4682b4ac35287c726fa35a983157a625902 (2026-09-18)"
  },
  "checked": 24,
  "verified": [
    "depot_tools/README.md:10-33 — four 'most important' tools (fetch, gclient, git cl, roll-dep) at 10-23; gn/autoninja/siso/ninja at 30-33 under the verbatim line 27 'Also, includes shell script/batch file for tools required to build chromium'.",
    "depot_tools/README.md:3,5 — 'Requires Python 3.8+.' and 'gsutil: Python 3.9-3.13 required.'",
    "depot_tools/gn.py:39-87 — FindGnTool: GetPrimarySolutionPath -> <solution>/third_party/gn/gn (44-51), GetBuildtoolsPlatformBinaryPath (55), FindInPath only when bin_path is falsy (56-59), buildtools gn/gn and gn candidates (74-81). Wrapper bundles no binary.",
    "depot_tools/gclient:8-29 — skip-list string '#grep#fetch#cleanup#diff#help#getdep#root#setdep#recurse#--help#'; DEPOT_TOOLS_UPDATE != 0 gate; line 29 execs vpython3 gclient.py.",
    "depot_tools/README.md:73-75 — DEPOT_TOOLS_UPDATE=0 / update_depot_tools_toggle.py --disable.",
    "src/DEPS:2211-2212 — 'src/third_party/depot_tools' pinned at 0306e4682b4ac35287c726fa35a983157a625902, identical to the depot_tools clone read here.",
    "src/DEPS:4456-4466 — hook 'disable_depot_tools_selfupdate' running update_depot_tools_toggle.py --disable.",
    "src/DEPS:295 — 'cpython3_version': 'version:3@3.11.9.chromium.38'.",
    "src/.gn:12 — script_executable = \"//third_party/cpython3/host/bin/python3\".",
    "src/vpython.toml:14 — requires-python = '>=3.11,<3.12'; header confirms it is the uv-backed replacement for //.vpython3.",
    "depot_tools/fetch_configs/chromium.py:15-38 — code excerpt quoted verbatim, byte for byte including indentation.",
    "depot_tools/fetch_configs/android.py:14-20 — props ['--target_os=android']; ios.py:18 — ['--target_os=ios', '--target_os_only=True']; chrome.py:14-22 — ['--internal=True'].",
    "depot_tools/fetch.py:142-166 — GclientGitCheckout.init (class at 113): gclient config --spec, sync, --nohooks / --no-history / --with_branch_heads, then git config diff.ignoreSubmodules dirty at 166.",
    "depot_tools/fetch.py:222-231 — --git-cache argument; :323-332 — setdefault cache_dir (GIT_CACHE_PATH or depot_tools cache) and cache_mode 'bootstrap'.",
    "'newest flag' — git log -G on fetch.py: --git-cache added e21955f71 2026-06-24, the most recent option added to fetch.py (previous: 3d401c263 2025-11-21, --protocol-override 2022-05-03).",
    "src/docs/linux/build_instructions.md:70-100 — 'fetch --nohooks chromium' (74); 'It mirrors every repo it fetches (~30 GB for Chromium)' (95); 'Expect the command to take 30 minutes on even a fast connection' (99).",
    "depot_tools/gclient_eval.py:699-774 — ParseLocalConfig: ast.parse, rejects non-Assign statements, one target, ast.Name target, no duplicate names.",
    "depot_tools/gclient.py:2215-2239 — eight solution keys: name, url, protocol_override, managed, custom_deps, custom_vars, custom_hooks, deps_file (default 'DEPS').",
    "depot_tools/gclient.py:2043-2059 — DEPS_OS_CHOICES, 'linux': 'unix'; :2160-2256 — SetConfig unions target_os/target_cpu with the enforced set, reads cache_dir, cache_mode and root hooks.",
    "depot_tools/gclient.py:2225 — managed=s.get(\"managed\", False).",
    "depot_tools/gclient_eval.py:557-559 — vars_override filtered by 'if k in vars_dict': custom_vars can only override an existing var.",
    "depot_tools/gclient.py:756-796 — _postprocess_deps: existing name overridden via _OverrideUrl, unknown name appended as a git dep, falsy/None value not added (acts as a revision override).",
    "depot_tools/gclient.py:1180-1190 — custom_hooks suppress DEPS hooks of the same name; those with an 'action' are appended.",
    "depot_tools/gclient.py:2338-2354 — _SaveEntries writes 'entries = {' plus one 'name: url' line per subtree entry, next to .gclient.",
    "depot_tools/gclient.py:2596-2751 (inside _RemoveUnversionedGitDirs, def at 2554) — orphan handling; verbatim \"WARNING: '%s' is no longer part of this client.\" at 2740 and 2748; deletion gated on delete_unversioned_trees (gclient sync -D).",
    "depot_tools/gclient_paths.py:21-72 — FindGclientRoot climbs to .gclient, returns early if path == real_from_dir, else parses .gclient_entries and walks path_to_check up until it matches a listed directory.",
    "depot_tools/gclient.py:1924-1940 — get_vars(); the precedence comment at 1927-1931 is 'DEPS vars / parents, from first to last / built-in / custom_vars overrides'.",
    "Figure counts 451 deps, 143 vars, 61 hooks at f288fed6 — reparsed with depot_tools/gclient_eval.Exec on src/DEPS: 451 / 143 / 61.",
    "managed dates — depot_tools 943f35a5f (22 Aug 2013, 'git: make fetch chromium use unmanaged mode', body 'work in a better, more predictable and robust way'); d4aedc811 (28 Jun 2016, 'Switch gclient config to default to unmanaged mode'); ae7ea3167 (18 Jul 2016, reverts four commits including d4aedc811, 'Reason for revert: Breaks iOS release branch builds.'); 7447d59a5 (16 Jun 2026, 'gclient: default to unmanaged solutions' — adds --managed, drops 'managed: False' from every fetch_config). 'Three weeks later' = 20 days; 'ten years later' = 2016 to 2026."
  ],
  "corrected": [
    "depot_tools/README.md:73-76 -> :73-75 — the Updating paragraph ends at line 75; line 76 is blank (depot_tools/README.md).",
    "depot_tools/gn.py:39-62 -> :39-87 — the cited range stopped mid-function; FindGnTool runs 39-87 and the buildtools candidates are at 74-81 (depot_tools/gn.py).",
    "'only then searches $PATH' -> 'reaches $PATH only when there is no buildtools directory at all' — gn.py calls FindInPath only when GetBuildtoolsPlatformBinaryPath returns falsy; when the buildtools path exists but holds no gn, it prints an error and returns None without consulting $PATH (depot_tools/gn.py:55-62, 74-87).",
    "'every subcommand except a hard-coded read-only list' -> 'every subcommand except nine named in a hard-coded string' with the nine listed — the string includes setdep, which writes DEPS, so 'read-only' was wrong (depot_tools/gclient:8).",
    "citation for the fetch aliases: android.py:14-20 alone -> android.py:14-20; ios.py:14-20; chrome.py:14-22 — the --target_os=ios/--target_os_only=True and --internal=True props are in the other two files (depot_tools/fetch_configs/).",
    "added citation for 'custom_vars overrides only an existing variable' -> gclient_eval.py:557-559, and for the custom_deps behaviours -> gclient.py:756-796; both were unbadged mechanism claims with no source (depot_tools).",
    "figure caption 'the comment above get_vars()' -> 'the comment inside get_vars()' — the precedence comment is at 1927-1931, inside the body (depot_tools/gclient.py)."
  ],
  "removed": [],
  "structure": {
    "h2": "exactly one <h2 id=\"the-tool-belt\"> with <span class=\"secnum\">{{S}}</span>",
    "figure_label": "Figure {{N}}",
    "figures": "fig-three-files has id, figure-head, figure-body, figure-caption",
    "svg_markers": "fig-three-files-ink / -blue / -purple, all prefixed by the figure id",
    "escaping": "no raw <, > or & inside <code>/<pre>",
    "external_resources": "no <img>, <link> or <script>",
    "tag_balance": "clean"
  },
  "skipped_existing": false
}
